@model IEnumerable<Login.Models.Video>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<nav class="navbar navbar-default navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <a class="navbar-brand" href="#myPage">DRAGON GYM</a>
        </div>
        <div class="collapse navbar-collapse" id="myNavbar">
            <ul class="nav navbar-nav navbar-right">
                <li>@Html.ActionLink("Trang chủ", "Index", "Home")</li>
                <li>@Html.ActionLink("Khách hàng", "Index", "KHACHHANGs")</li>
                <li>@Html.ActionLink("Nhân viên", "Index", "NHANVIENs")</li>
                <li>@Html.ActionLink("Thiết bị", "Index", "THIETBIs")</li>
                <li>@Html.ActionLink("Khuyến mãi", "Index", "KHUYENMAIs")</li>
                <li  class="active">@Html.ActionLink("Videos", "Index", "Videos")</li>
            </ul>
        </div>
    </div>
</nav>

<div class="container-fluid" style=" background-image: url('/Content/images/background.jpg'); width: 100%; height:750px;">

    <div class="table-wrapper">
        <div class="table-title">
            <div class="row">
                <div class="col-sm-8"><h1 style="color: whitesmoke">QUẢN LÝ VIDEO</h1></div>
            </div>
            <button type="button" class="btn btn-default " style="color:black">
                <i class="fa fa-plus" aria-hidden="true"></i> @Html.ActionLink("Thêm mới", "Create")
            </button>

            <button type="button" class="btn btn-default " style="color:black" id="do-exercise">
                <i class="fa fa-plus" aria-hidden="true"></i> @Html.ActionLink("Tap the duc", "DoExercise")
            </button>
            <br />

        </div>
        <br />

        <table class="table table-bordered">
            <tr class="table">
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Url)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Type)
                </th>
                <th></th>
            </tr>

            @foreach (var item in Model)
            {
                <tr class="table">
                    <td>
                        @Html.DisplayFor(modelItem => item.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Url)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Type)
                    </td>
                    <td>
                        <button type="button" class="btn btn-default" style="font-size: large;color:black">
                            <i class="fa fa-wrench" aria-hidden="true"></i> @Html.ActionLink("Sửa", "Edit", new { id = item.Id })
                        </button>
                        <button type="button" class="btn btn-default" style="font-size: large;color:black">
                            <i class="fa fa-info" aria-hidden="true"></i> @Html.ActionLink("Chi tiết", "Details", new { id = item.Id })
                        </button>
                        <button type="button" class="btn btn-default" style="font-size:large;color:black">
                            <i class="fa fa-trash-o" aria-hidden="true"></i> @Html.ActionLink("Xóa", "Delete", new { id = item.Id })
                        </button>

                    </td>
                </tr>
            }

        </table>
        </div>
</div>